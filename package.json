{
  "name": "gha-workflow-filter",
  "version": "1.1.0",
  "description": "GitHub Actionsのワークフロー一覧ページで、左サイドバーのワークフローを表示名でリアルタイムにフィルタリングできるChrome拡張です。",
  "type": "module",
  "scripts": {
    "build": "esbuild src/init.js --bundle --outfile=dist/content.js --format=iife --platform=browser",
    "package": "npm run build && zip -r gha-workflow-filter.zip manifest.json content.css icons/ dist/",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "esbuild": "0.28.1",
    "happy-dom": "20.10.6",
    "vitest": "4.1.9"
  }
}
